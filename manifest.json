{
  "version": 1,

  "agent": {
    "name": "Staging Template",
    "description": "A personal AI assistant that's actually helpful. Reads files, searches the web, manages memory, and gets things done without asking unnecessary questions.",
    "vibe": "resourceful, opinionated, gets things done",
    "emoji": "🧠"
  },

  "template": {
    "slug": "staging-template",
    "category": "general",
    "tags": ["assistant", "personal", "productivity", "general"],
    "partnerName": "Pinata"
  },

  "secrets": [
    {
      "name": "MY_SECRET",
      "description": "Example secret — any secret your agent might need to work",
      "required": false
    }
  ],

  "scripts": {
    "build": "cd ./workspace/projects/hello-world && npm install --include=dev && npm run build",
    "start": "cd ./workspace/projects/hello-world && npx vite preview --host 0.0.0.0 --port 5173"
  },

  "routes": [
    {
      "port": 5173,
      "path": "/app",
      "protected": false
    }
  ],

  "tasks": [
    {
      "name": "daily-check",
      "prompt": "Run your daily check-in routine",
      "schedule": "0 9 * * *",
      "enabled": true
    }
  ]
}
